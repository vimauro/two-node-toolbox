---
# Prerequisites check and installation for kcli-install

# Setup libvirt virtualization infrastructure  
- name: Setup libvirt infrastructure
  include_tasks: libvirt_setup.yml

# Install and configure kcli
- name: Install and configure kcli
  include_tasks: kcli_install.yml

# Setup user environment and SSH access
- name: Setup user environment
  include_role:
    name: config
  vars:
    ansible_skip_tags: git-setup 